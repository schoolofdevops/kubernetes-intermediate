apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: result-route
  namespace: default
spec:
  parentRefs:
  - name: voting-app-gateway
  hostnames:
  - "result.example.com"
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: result
      port: 80
